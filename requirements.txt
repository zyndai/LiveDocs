# Haystack implementation -- install with:
#   pip install -r ragpipline/requirements.txt
#
# Coexists with the root requirements.txt. Same venv is fine; these are
# additive packages, not replacements.

tiktoken
sentence-transformers>=5.0
accelerate>=0.30
haystack-ai>=2.0
qdrant-haystack>=5.0
fastembed-haystack>=1.0
google-genai-haystack>=2.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn[standard]>=0.27
