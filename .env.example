GOOGLE_API_KEY=paste-your-google-ai-studio-key-here

# Required only if DOCS_REPO_URL points at a PRIVATE repo over HTTPS.
# Create a fine-grained PAT at https://github.com/settings/tokens?type=beta
# Scope: "Contents: Read-only" on the docs repo. No other permissions needed.
# Leave blank/unset for public repos or when using SSH URLs.

